# PyTorch CPU-only wheels — avoids downloading CUDA libraries (~1.9 GB) on CPU-only VPS.
# SynapseOS uses llama.cpp for LLM inference; PyTorch is only needed for BGE embeddings.
# GPU users: replace torch==2.2.1+cpu with torch==2.2.1 and remove this line.
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Core framework ───────────────────────────────────────────────────────────
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.3
pydantic-settings==2.2.1
python-dotenv==1.0.1
python-multipart==0.0.9

# ── HTTP / async ─────────────────────────────────────────────────────────────
httpx==0.27.0
aiohttp==3.9.3
websockets==12.0
aiofiles==23.2.1

# ── ML / embeddings ──────────────────────────────────────────────────────────
sentence-transformers==2.6.1
transformers==4.39.1
rank-bm25==0.2.2
torch==2.2.1+cpu
numpy==1.26.4
scipy==1.12.0

# ── Vector database ──────────────────────────────────────────────────────────
faiss-cpu==1.8.0

# ── Database ─────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.28
aiosqlite==0.20.0

# ── Token counting ───────────────────────────────────────────────────────────
tiktoken==0.6.0

# ── Resilience / retry ───────────────────────────────────────────────────────
tenacity==8.2.3

# ── Logging / observability ──────────────────────────────────────────────────
structlog==24.1.0
rich==13.7.0
prometheus-client==0.20.0

# ── CLI tooling ──────────────────────────────────────────────────────────────
click==8.1.7
typer==0.9.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.5
pytest-cov==5.0.0
httpx==0.27.0

# ── Optional expert models ───────────────────────────────────────────────────
# openai-whisper==20231117    # Uncomment for Whisper ASR
# TTS==0.22.0                 # Uncomment for Piper/Coqui TTS
# sacremoses==0.1.1           # Uncomment for MarianMT translation
